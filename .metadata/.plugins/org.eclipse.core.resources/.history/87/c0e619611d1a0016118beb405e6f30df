/*
 ============================================================================
 Name        : josephusProblem.c
 Author      : Natasha Kaweski e Vinícius Ferreira
 Version     :
 Copyright   : Your copyright notice
 Description : O Problema de Josephus (Tenenbaum, 1989)
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

typedef struct soldado SLista;
struct soldado {
	int numero;
	SLista* proximo;
};

insereSoldadoNoCirc(int num, SLista* lista);

int main(void) {

	/*
	 * Incializa-se a lista.
	 */
	SLista* circuloSoldados = queComeceOJogo();

	/*
	 * Entrada de Dados:
	 * Pede ao usuário que informe um número num de soldados a serem inseridos na lista.
	 */

	int numSoldados = 0;
	printf("O Problema de Josephus\n\nNúmero de soldados utilizados:");
	scanf("%d", &numSoldados);

	/*
	 * Cria uma matriz com o número de soldados a serem inseridos na lista
	 */
	char soldados[numSoldados][15];

	// Insere esse numero de soldados na lista
	insereSoldadoNoCirc(numSoldados, circuloSoldados);

	/*
	 * Fim do programa.
	 */
	return 0;
}

/*
 * Função: Inicializa a lista circular.
 */
SLista* queComeceOJogo() {
	return NULL;
}

/*
 * Return
 * Param
 */

SLista* insereSoldadoNoCirc(int num, SLista* lista) {
	return lista;
}

/*
 * Return
 * Param
 */
//verificaCircVazio();

/*
 * Return
 * Param
 */
//imprimeSoldadosCirc();

/*
 * Return
 * Param
 */
//verificaQteSoldados();

/*
 * Return:
 * Param
 */
//executaJosephus();
